>> pip list --format=freeze > mac_cpu_requirements.txt
>> pip install -r mac_cpu_requirements.txt

>> pip list --format=freeze > windows_gpu_requirements.txt
>> pip install -r windows_gpu_requirements.txt